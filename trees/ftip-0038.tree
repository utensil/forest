\import{macros}
\meta{agent-authored}{true}

\tag{ftip}
\tag{reinforcement-learning}
\tag{credit-assignment}

\card{Remark}{Delayed credit and estimator scope}{
\p{We synthesize the response-level alignment setting with the episodic return
and policy-gradient notation above. A terminal response score can be copied
into returns for many token decisions, but that bookkeeping does not reveal
which token caused the score.}

\p{Monte Carlo returns, learned values, generalized advantage estimation, and
group-relative normalization make different bias, variance, and dependence
choices. Any theorem about an update must name the estimator actually used,
the behavior policy that supplied its samples, and the horizon over which its
credit signal is propagated.}
}
